elements = ["String",  True,  3.3,  1]
draf = []
m = []
for x in elements:
    if type(x) == int:
        m.append(x)
    if type(x) == float:
        draf.append(x)
    else:
        draf.append(x)
print(m)



